{
  "name": "kapture-collections-voicebot-mock-server",
  "version": "1.0.0",
  "private": true,
  "description": "Mock Vapi tool webhook for Kapture Finance collections demo",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": { "express": "^4.21.2", "dotenv": "^16.4.7" }
}
