services:
  - type: web
    name: kapture-collections-voicebot
    runtime: node
    rootDir: mock-server
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
